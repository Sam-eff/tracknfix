# Native localhost frontend overrides.
# Copy this file to frontend/.env.local when you want Vite to proxy to a
# Django server running directly on your laptop.

VITE_DEV_BACKEND_URL=http://127.0.0.1:8000
